name: dart_osgi_flutter
description: Flutter-specific OSGi helpers — FlutterBundleContext, OsgiBundleApp.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0
  flutter: ">=3.0.0"

dependencies:
  dart_osgi_api:
  flutter:
    sdk: flutter

resolution: workspace
