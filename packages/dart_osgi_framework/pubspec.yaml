name: dart_osgi_framework
description: OSGi framework implementation — registry, event admin, isolate bus.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  dart_osgi_api:
  yaml: ^3.1.0
  pub_semver: ^2.1.0

dev_dependencies:
  test: ^1.25.0

resolution: workspace
