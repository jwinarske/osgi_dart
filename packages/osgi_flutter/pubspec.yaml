name: osgi_flutter
description: >
  Flutter integration for the Dart OSGi framework: the MethodChannel shell
  transport, and the widgets a bundle needs to surface its own lifecycle.
version: 0.1.0
repository: https://github.com/jwinarske/dart_osgi
publish_to: none

environment:
  sdk: ^3.12.0
  flutter: ">=3.27.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  osgi_api: ^0.1.0

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
