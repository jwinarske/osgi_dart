name: osgi_flutter
description: >
  Flutter integration for the Dart OSGi framework: the MethodChannel shell
  transport.
version: 0.1.0
repository: https://github.com/jwinarske/osgi_dart
publish_to: none

environment:
  sdk: ^3.12.0
  flutter: ">=3.27.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  osgi_api: ^0.1.0

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
